# Horai

Give the current date, time, season, and time-of-day.

- **UI:** Chronometry view (also drives the header clock)
- **Brain tool:** `get_time_context`
- **Files:** `tools/horai.py`, `panels/horai_panel.py`
- **Test:** `python -X utf8 -m unittest tests.test_horai`

Details: see the docstring in `tools/horai.py`.
